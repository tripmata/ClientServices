<?php

	$ret = new stdClass();

                    if($GLOBALS['user']->Id != "")
                    {
                        

                        if($GLOBALS['user']->Role->Messaging->ReadAccess)
                        {
                            $ret->data = new Message($GLOBALS['subscriber']);
                            $ret->data->Initialize($_REQUEST['messageid']);
                            $ret->data->markOpened();

                            $ret->status = "success";
                            $ret->message = "Message has been retrived successfully";
                        }
                        else
                        {
                            $ret->status = "access denied";
                            $ret->message = "You do not have the required privilege to complete the operation";
                        }
                    }
                    else
                    {
                        $ret->status = "login";
                        $ret->data = "login & try again";
                    }

	echo json_encode($ret);